# Infrastructure

`infra-configs` is the infrastructure desired-state and operational-knowledge monorepo. Infrastructure changes use Linear for work tracking, GitHub for reviewed implementation history, and approval-gated deployment.

The Proxmox learning lab is maintained under `infra-configs/labs/proxmox` as a separate lifecycle boundary.
