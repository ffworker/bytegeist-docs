# Linear

Linear is the canonical system for work, tickets, ideas, triage, status, and completion. A GitHub PR implements a Linear item; it does not replace it.

Infrastructure work may use `infra-configs` paths and PRs, but deployment remains approval-gated.
